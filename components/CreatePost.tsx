'use client';import {useState} from 'react';
export function CreatePost({refresh}:{refresh:()=>void}){const [content,setContent]=useState('');const [imageUrl,setImageUrl]=useState('');const [busy,setBusy]=useState(false);const submit=async(e:any)=>{e.preventDefault();if(!content.trim()&&!imageUrl)return;setBusy(true);await fetch('/api/posts',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({content,imageUrl:imageUrl||null})});setContent('');setImageUrl('');setBusy(false);refresh()};return <form className="card" style={{padding:14,marginBottom:16}} onSubmit={submit}><textarea className="input" rows={3} placeholder="Share something…" value={content} onChange={e=>setContent(e.target.value)}/><div style={{display:'flex',gap:8,marginTop:8}}><input className="input" placeholder="Optional image URL" value={imageUrl} onChange={e=>setImageUrl(e.target.value)}/><button className="btn btn-primary" disabled={busy}>Post</button></div></form>}
