'use client';
import Link from 'next/link';import {Home,MessageCircle,User,Settings,Bell} from 'lucide-react';import {usePathname} from 'next/navigation';import {useEffect,useState} from 'react';
export function AppShell({children}:{children:React.ReactNode}){const p=usePathname();const [me,setMe]=useState<any>(null);useEffect(()=>{fetch('/api/auth/me').then(r=>r.json()).then(x=>setMe(x.user))},[]);if(p?.startsWith('/login')||p?.startsWith('/signup'))return <>{children}</>;return <div className="shell"><header style={{height:60,borderBottom:'1px solid var(--line)',background:'var(--surface)',display:'flex',alignItems:'center',justifyContent:'space-between',padding:'0 16px',position:'sticky',top:0,zIndex:10}}><b style={{fontSize:22}}>QEVLI</b>{me&&<Link href="/profile" style={{textDecoration:'none',color:'var(--text)'}}>{me.name}</Link>}</header><main className="content">{children}</main><nav className="nav"><Link className={p==='/'?'active':''} href="/"><Home size={20}/>Home</Link><Link className={p?.startsWith('/messages')?'active':''} href="/messages"><MessageCircle size={20}/>Messages</Link><Link className={p?.startsWith('/profile')?'active':''} href="/profile"><User size={20}/>Profile</Link><Link href="/notifications"><Bell size={20}/>Alerts</Link><Link className={p?.startsWith('/settings')?'active':''} href="/settings"><Settings size={20}/>Settings</Link></nav></div>}
