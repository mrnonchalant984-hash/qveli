"use client";
import Link from "next/link";import {ArrowLeft,Eye,EyeOff,LockKeyhole,Sparkles} from "lucide-react";import {useState} from "react";
export default function Login(){const [show,setShow]=useState(false),[identifier,setIdentifier]=useState(''),[password,setPassword]=useState(''),[msg,setMsg]=useState(''),[remember,setRemember]=useState(false),[busy,setBusy]=useState(false);const submit=async(e:React.FormEvent)=>{e.preventDefault();setMsg('');setBusy(true);try{const r=await fetch('/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email:identifier,username:identifier,password,remember})});const j=await r.json();if(!r.ok){setMsg(j.error||'Login failed.');return}location.href='/dashboard'}catch{setMsg('Unable to connect to Qevli.')}finally{setBusy(false)}};return <main className="authPage"><div className="authBrand"><Link href="/"><span className="qIcon">Q</span> Qevli</Link><Link href="/" className="back"><ArrowLeft size={16}/> Back to Qevli</Link></div><div className="authLayout"><section className="authPitch"><div className="eyebrow"><Sparkles size={15}/> Your world, your people</div><h1>Everything you care about, in one place.</h1><p>Connect with people, share moments and build your own corner of Qevli.</p></section><section className="authCard"><div className="authHeading"><div className="lockIcon"><LockKeyhole size={20}/></div><h2>Welcome to Qevli</h2><p>Log in to continue to your world.</p></div><form onSubmit={submit}><label>Email or username<input required value={identifier} onChange={e=>setIdentifier(e.target.value)} placeholder="you@example.com"/></label><label>Password<div className="pass"><input required value={password} onChange={e=>setPassword(e.target.value)} type={show?'text':'password'} placeholder="Enter your password"/><button type="button" onClick={()=>setShow(!show)}>{show?<EyeOff size={18}/>:<Eye size={18}/>}</button></div></label><div className="formRow"><label className="check"><input type="checkbox" checked={remember} onChange={e=>setRemember(e.target.checked)}/> Remember me</label><Link href="/forgot-password">Forgot password?</Link></div><button className="primaryBtn full" disabled={busy} type="submit">{busy?'Logging in…':'Log in'}</button>{msg&&<div className="demoNotice">{msg}</div>}</form><div className="authRealNote">Qevli uses your real Qevli account. Social sign-in will only appear when a provider is actually configured.</div><p className="authFoot">New to Qevli? <Link href="/signup">Create an account</Link></p></section></div></main>}
function Divider(){return <div className="divider"><span>or</span></div>}
