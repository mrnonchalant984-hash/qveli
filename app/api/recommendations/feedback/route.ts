import { NextRequest } from 'next/server'; import { getCurrentUser } from '@/lib/auth'; import { prisma } from '@/lib/prisma'; import { bad, created, unauthorized, serverError } from '@/lib/http';
export async function POST(req:NextRequest){try{const u=await getCurrentUser();if(!u)return unauthorized();const b=await req.json();if(!b.targetType||!b.targetId||!b.action)return bad('Missing recommendation feedback.');const row=await prisma.recommendationFeedback.create({data:{userId:u.id,targetType:String(b.targetType),targetId:String(b.targetId),action:String(b.action)}});return created({feedback:row});}catch{return serverError();}}
