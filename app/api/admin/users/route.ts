import { getCurrentUser, publicUser } from '@/lib/auth'; import { prisma } from '@/lib/prisma'; import { bad, ok, unauthorized, serverError } from '@/lib/http';
export async function GET(){try{const me=await getCurrentUser();if(!me)return unauthorized();if(me.role!=='ADMIN')return bad('Admin access required.',403);const users=await prisma.user.findMany({select:{id:true,email:true,username:true,name:true,role:true,createdAt:true,lastSeenAt:true,professionalMode:true,verified:true,verifiedAt:true,_count:{select:{posts:true,followers:true,following:true}}},orderBy:{createdAt:'desc'},take:200});return ok({users});}catch{return serverError();}}
