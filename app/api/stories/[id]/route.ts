import { getCurrentUser } from '@/lib/auth'; import { prisma } from '@/lib/prisma'; import { ok, unauthorized, notFound, bad, serverError } from '@/lib/http';
export async function DELETE(_:Request,{params}:{params:Promise<{id:string}>}){try{const me=await getCurrentUser();if(!me)return unauthorized();const {id}=await params;const s=await prisma.story.findUnique({where:{id}});if(!s)return notFound('Story not found.');if(s.authorId!==me.id&&me.role!=='ADMIN')return bad('You cannot delete this story.',403);await prisma.story.delete({where:{id}});return ok({deleted:true});}catch{return serverError();}}
