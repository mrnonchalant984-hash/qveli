import { NextRequest } from 'next/server'; import { getCurrentUser } from '@/lib/auth'; import { prisma } from '@/lib/prisma'; import { bad, ok, unauthorized, serverError } from '@/lib/http';
export async function PATCH(req:NextRequest,{params}:{params:Promise<{id:string}>}){try{const u=await getCurrentUser();if(!u)return unauthorized();if(u.role!=='ADMIN')return bad('Admin access required.',403);const {id}=await params;const b=await req.json();const row=await prisma.moderationCase.update({where:{id},data:{status:b.status?String(b.status):undefined,severity:b.severity?String(b.severity):undefined,resolution:b.resolution?String(b.resolution):undefined}});return ok({case:row});}catch{return serverError();}}
